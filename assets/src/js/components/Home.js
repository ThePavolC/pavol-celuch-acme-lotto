import React from "react";
import { Container } from "react-bootstrap";

export default function Home() {
    return (
        <Container>
            <h1>Home page</h1>
            <p>Basic client app build with React.</p>
        </Container>
    );
}
